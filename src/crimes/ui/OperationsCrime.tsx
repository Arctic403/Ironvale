import React from "react";
import { Button } from "../../components/ui";
import { GameIcon } from "../../components/GameIcon";
import { formatTime, money } from "../../core/gameCore";
import { CRIME_FAMILY_LABELS, CRIME_OPERATIONS } from "../../systems/crimeActivities";
import type { CrimeCareerDefinition } from "../core/types";
import { CrimeRequiredItems } from "./CrimeRequiredItems";
import type { CrimeGame, FeedbackRenderer, WithCrimeFeedback } from "./types";

export function OperationsCrime({g,career,now,incapacitated,feedbackBusy,withCrimeFeedback,renderFeedback}:{g:CrimeGame;career:CrimeCareerDefinition;now:number;incapacitated:boolean;feedbackBusy:boolean;withCrimeFeedback:WithCrimeFeedback;renderFeedback:FeedbackRenderer}) {
  const operations=(career.operationIds??[]).map((id)=>CRIME_OPERATIONS.find((item)=>item.id===id)??null).filter((item):item is NonNullable<typeof item>=>Boolean(item));
  return <div className="crime-detail-stack"><div className="crime-mechanic-note"><GameIcon name="clock" size={16}/><div><strong>PASSIVE OPERATION</strong><span>These keep running while you leave the page. Some have fixed completion timers; risk-build operations let you cash out early while value and detection pressure climb together.</span></div></div>{operations.map((operation)=>{const active=g.gameState.activeCrimeOperations.find((job)=>job.operationId===operation.id);const remaining=active?Math.max(0,active.finishesAt-now):0;const progress=active?Math.max(0,Math.min(1,(now-active.startedAt)/Math.max(1,active.finishesAt-active.startedAt))):0;const ready=Boolean(active&&remaining<=0);const riskBuild=operation.cashoutMode==="risk-build";const canCash=Boolean(active&&riskBuild&&progress>=(operation.minCashoutProgress??.15));const liveDetection=Math.max(1,Math.round(operation.detectionRisk*(riskBuild?(.35+progress*.9):1)));const accruedMin=Math.round(operation.minReward*(riskBuild?(.22+progress*.78):1));const accruedMax=Math.round(operation.maxReward*(riskBuild?(.22+progress*.78):1));const missing=(operation.requiredItems??[]).filter((id)=>(g.gameState.inventory[id]||0)<=0);const key=`operation:${operation.id}`;return <article key={operation.id} className={`crime-operation-card career-operation ${ready?"ready":""}`}><header><span><GameIcon name={operation.icon} size={19}/></span><div><small>{CRIME_FAMILY_LABELS[operation.family]}</small><h4>{operation.name}</h4></div></header><p>{operation.description}</p><div className="operation-metrics"><span><small>Setup</small><strong>{money(operation.setupCost)}</strong></span><span><small>{riskBuild?"Value built":"Timer"}</small><strong>{active&&riskBuild?`${Math.round(progress*100)}%`:formatTime(operation.durationMs)}</strong></span><span><small>{active&&riskBuild?"Accrued range":"Payout"}</small><strong>{money(active&&riskBuild?accruedMin:operation.minReward)}–{money(active&&riskBuild?accruedMax:operation.maxReward)}</strong></span><span><small>Detection</small><strong>{active&&riskBuild?`${liveDetection}% live`:`${operation.detectionRisk}% base`}</strong></span></div>{active&&riskBuild?<div className="operation-risk-build"><div><i style={{width:`${progress*100}%`}}/></div><small>Waiting longer increases the possible payout, but detection pressure also grows.</small></div>:null}<CrimeRequiredItems ids={operation.requiredItems} inventory={g.gameState.inventory}/>{active?<Button disabled={(riskBuild?!canCash:!ready)||feedbackBusy} onClick={()=>withCrimeFeedback({key,crimeId:career.id,subject:operation.name,actionLabel:riskBuild&&remaining>0?"Cash out operation":"Collect operation",label:riskBuild&&remaining>0?"CASHING OUT…":"COLLECTING RESULT…"},()=>g.claimCrimeOperation(active.id))}>{riskBuild?(canCash?(remaining>0?"Cash Out Now":"Collect Full Run"):`Building Value · ${Math.round(progress*100)}%`):(ready?"Collect Result":`Running · ${formatTime(remaining)}`)}</Button>:<Button disabled={missing.length>0||incapacitated||g.gameState.cash<operation.setupCost||g.gameState.nerve<operation.nerve||feedbackBusy} onClick={()=>withCrimeFeedback({key,crimeId:career.id,subject:operation.name,actionLabel:"Launch operation",label:"SETTING UP OPERATION…"},()=>g.startCrimeOperation(operation.id))}>Launch Operation</Button>}{renderFeedback(key)}</article>;})}</div>;
}
