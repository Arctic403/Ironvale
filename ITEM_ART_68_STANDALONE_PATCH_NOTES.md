# 68 Standalone Item Art Patch

All 68 item assets now use individually generated standalone artwork. Existing item IDs and `/assets/items/<id>.webp` paths are preserved, so current item UI consumers keep working without code changes.

The atlas reference image is retained for reference only.
