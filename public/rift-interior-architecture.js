// H2.00 authoritative staged interior pipeline.
// This path is intentionally retained so every existing RiftCity import now resolves
// to the researched topology -> reservation -> circulation -> interior architecture flow.
export {
  RIFT_INTERIOR_ARCHITECTURE_VERSION,
  RIFT_INTERIOR_MIN_CLEAR_HEIGHT,
  planRiftInteriorArchitecture,
  reserveRiftInteriorArchitecture,
  realizeRiftInteriorArchitecture,
  prepareRiftInteriorArchitecture,
  validateRiftInteriorArchitecture
} from './rift-interior-pipeline.js';
