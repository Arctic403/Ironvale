// Optional source-authored RiftCity building assets.
//
// Runtime imports from RiftAssets are stored as complete asset-pack objects by
// the Block Editor. Keep this registry for assets that are deliberately shipped
// with the game source later; do not bake one-off Asset Lab exports in here.
export const BLOCK_ASSETS = Object.freeze({});
